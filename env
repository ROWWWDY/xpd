# This file is for LOCAL development only (used by `vercel dev`).
# In production, set these same names as real Environment Variables in the
# Vercel dashboard (Project → Settings → Environment Variables) — do NOT
# commit a real .env file to GitHub.

ADMIN_USER=Thakkali
ADMIN_PASS=Thakkalibiju144

# Any long random string — signs the admin login cookie.
JWT_SECRET=replace-with-a-long-random-string

# From the Upstash Redis integration (Vercel dashboard → Storage → Marketplace
# Database Integrations → Upstash). It auto-fills these for you in production;
# for local dev, run: vercel env pull .env.development.local
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
